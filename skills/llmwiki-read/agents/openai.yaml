interface:
  display_name: "LLMwiki Read"
  short_description: "LLMwiki Viewerを検索して根拠ページを読み取り"
  default_prompt: "Use $llmwiki-read to search the LLMwiki Viewer and read the relevant pages without changing wiki state."

policy:
  allow_implicit_invocation: true
