interface:
  display_name: "LLMwiki Read"
  short_description: "LLMwikiの根拠・ページ・状態を読み取り専用で取得"
  default_prompt: "Use $llmwiki-read to find grounded evidence in LLMwiki without changing wiki state."

policy:
  allow_implicit_invocation: true
