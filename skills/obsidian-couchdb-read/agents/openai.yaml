interface:
  display_name: "Obsidian CouchDB Read"
  short_description: "CouchDB上のObsidianノートを安全に検索・取得"
  default_prompt: "Use $obsidian-couchdb-read to search the read-only Obsidian CouchDB and cite the matching note paths."

policy:
  allow_implicit_invocation: true
