[project]
name = "agent-skills"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["openai>=2.8.1", "python-dotenv>=1.2.1", "requests>=2.32.5"]

[dependency-groups]
dev = ["pytest>=9.1.1", "ruff>=0.15.22", "ty>=0.0.61"]
